import { Typography, Box } from '@mui/material'

const NotFound = (test, yes) => (
  <Box textAlign="center" alignItems="center" justifyContent="center">
    <Typography>404 | Page not found</Typography>
  </Box>
)

export default NotFound
